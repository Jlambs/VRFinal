using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This will one day be used as a centralized script that controls colors at all time.
// Will consolidate the need to update RGB color values on every applicable object if
// you want to redefine one of the "global" colors.

// For now, each Controller script manages its own colors, including conversions to/from
// different formats such as 'white' <=> (0, 0, 0) <=> Color whiteColor

public class ColorController : MonoBehaviour
{

}
